{% extends 'base.html.twig' %}

{% block title %}OlivetoDetails{% endblock %}

{% block body %}
    <h1>OlivetoDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oliveto_detail.id }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ oliveto_detail.categoria }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ oliveto_detail.note }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ oliveto_detail.data ? oliveto_detail.data|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ oliveto_detail.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('oliveto_details_index') }}">back to list</a>

    <a href="{{ path('oliveto_details_edit', {'id': oliveto_detail.id}) }}">edit</a>

    {{ include('oliveto_details/_delete_form.html.twig') }}
{% endblock %}
