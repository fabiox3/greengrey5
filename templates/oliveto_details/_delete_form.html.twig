<form method="post" action="{{ path('oliveto_details_delete', {'id': oliveto_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oliveto_detail.id) }}">
    <button class="btn">Delete</button>
</form>
