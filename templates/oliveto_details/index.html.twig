{% extends 'base.html.twig' %}

{% block title %}OlivetoDetails index{% endblock %}

{% block content %}
    <h1>OlivetoDetails index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categoria</th>
                <th>Note</th>
                <th>Data</th>
                <th>Rating</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for oliveto_detail in oliveto_details %}
            <tr>
                <td>{{ oliveto_detail.id }}</td>
                <td>{{ oliveto_detail.categoria }}</td>
                <td>{{ oliveto_detail.note }}</td>
                <td>{{ oliveto_detail.data ? oliveto_detail.data|date('Y-m-d') : '' }}</td>
                <td>{{ oliveto_detail.rating }}</td>
                <td>
                    <a href="{{ path('oliveto_details_show', {'id': oliveto_detail.id}) }}">show</a>
                    <a href="{{ path('oliveto_details_edit', {'id': oliveto_detail.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('oliveto_details_new') }}">Create new</a>
{% endblock %}
