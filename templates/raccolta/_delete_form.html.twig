<form method="post" action="{{ path('raccolta_delete', {'id': raccoltum.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questo record?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raccoltum.id) }}">
    <button class="btn btn-danger btn-sm">Cancella</button>
</form>
